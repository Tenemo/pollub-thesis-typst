#let sample-chapter = [
= Sample chapter

This chapter exists only to prove that the starter template can be split into
multiple files without duplicating layout code.

== Next steps

Replace the sample metadata in `main.typ`, rewrite these placeholder chapters,
and then keep the package API stable while you iterate on styling in the
template repository.
]
